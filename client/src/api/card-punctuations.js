// TODO: Because now we are now able to communicate with backend.
// import socket from './socket';

/* Actions */

/*
// TODO: Because now we are now able to communicate with backend.

const createCardPunctuations = (cardId, data, headers) =>
  socket.post(`/cards/${cardId}/punctuations`, data, headers);

const deleteCardPunctuations = (cardId, punctuationId, headers) =>
  socket.delete(`/cards/${cardId}/punctuations/${punctuationId}`, undefined, headers);

export default {
  createCardPunctuations,
  deleteCardPunctuations,
};
*/
