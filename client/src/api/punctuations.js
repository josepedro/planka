// TODO: Because now we are now able to communicate with backend.
// import socket from './socket';

/* Actions */

/*
// TODO: Because now we are now able to communicate with backend. =D

const createPunctuation = (boardId, data, headers) =>
  socket.post(`/boards/${boardId}/punctuations`, data, headers);

const updatePunctuation = (id, data, headers) => socket.patch(`/punctuations/${id}`, data, headers);

const deletePunctuation = (id, headers) => socket.delete(`/punctuations/${id}`, undefined, headers);

export default {
  createPunctuation,
  updatePunctuation,
  deletePunctuation,
};

*/
